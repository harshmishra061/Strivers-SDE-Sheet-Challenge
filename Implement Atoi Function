#include <bits/stdc++.h> 
int atoi(string str) {
    int res = 0;
    for(int i = 0;str[i];i++) {
        if(str[i] >= '0' && str[i] <= '9') {
            res = res * 10 + (str[i] - '0');
        }
    }
    if(str[0] == '-') res *= -1;
    return res;
}
